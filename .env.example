# Letta Configuration
LETTA_BASE_URL=http://localhost:8283

# Database: run scripts/setup_postgres_for_letta.py first, then use:
# LETTA_PG_URI=postgresql+asyncpg://letta:letta@localhost:5432/letta?ssl=false
# Start server with: ./run_server.sh  (loads .env so this URI is used)

# Agent ID (set after running setup_agent.py)
KNOWLEDGE_ENGINE_AGENT_ID=

# Obsidian Vault Path
OBSIDIAN_VAULT_PATH=/Users/marcuswong/Documents/KnowledgeVault

# API Keys (required by Letta for LLM calls)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=  # For embeddings
